# CHANGELOG — DLDM Project

_Every change to this project must be logged here. Format: `- **[Author]** Description. (Files: ...)`_

---

### 2026-08-09

- **[Adam]** Initialized project structure: created folder hierarchy, `CLAUDE.md`, `AGENTS.md`, `CLAUDE-MAP.md`, `CHANGELOG.md`, and `.agents/skills/` with changelog and folder-summary skills. All folder READMEs stubbed. (Files: all root-level files and subfolder READMEs)
- **[AI (via Adam)]** Created data retrieval research document covering: YouTube Data API v3 quota model and collection strategy, CC licensing filtering and sampling, frame extraction tools (decord/OpenCV/ffmpeg), transcript handling (auto-captions + Whisper fallback), and metadata feature mapping. (Files: `01_Research/2026-08-09_initial_research/data_retrieval.md`)

### 2026-08-10

- **[AI (via Adam)]** Created comprehensive evaluation, baselines, and project planning research document. Covers: (1) metrics for imbalanced binary classification (AUC-ROC primary, F1/PR-AUC supplementary, accuracy ceiling strategies), (2) baseline models and Wu et al. (2018) codebase adaptability assessment, (3) validation strategy (60/20/20 stratified split, 5-fold CV, time-based split considerations), (4) ablation study design (remove-one with 4 priority ablations), (5) comparable projects and realistic performance targets (AUC-ROC 0.70–0.78), (6) detailed 4-week project plan with task assignments, critical path analysis, risk register, and minimum viable submission definition. (Files: `01_Research/2026-08-09_initial_research/evaluation_and_planning.md`)
- **[AI (via Adam)]** Created comprehensive model architectures research document. Covers: (1) text encoder comparison (BERT/RoBERTa/DistilBERT/ModernBERT — recommended: ModernBERT-base for its 8K context, 2–4x speed, and Pareto improvements), (2) vision backbone comparison (ResNet/ViT/CLIP/DINOv2 — recommended: DINOv2 ViT-S/14 frozen), (3) temporal transformer options (YouTube-8M pooling, TimeSformer, VideoMAE, per-frame DINOv2 + aggregator — recommended: shared frozen DINOv2 + attention pooling), (4) fusion strategy (early/mid/late — recommended: late fusion with projection layers), (5) attribution/explainability (attention weights, Integrated Gradients via Captum, SHAP, LIME — recommended: Integrated Gradients as primary), (6) practical GPU memory (~3–5 GB frozen, ~6–8 GB fine-tuned) and 4-week timeline feasibility. Includes 3-tier recommendation stack (pragmatic → moderate → ambitious) with explicit scaling-down flags and fallback options. (Files: `01_Research/2026-08-09_initial_research/model_architectures.md`)
- **[Adam]** Created master milestones to-do list (`MILESTONES.md`) and executive summary (`SUMMARY.md`) synthesizing all three parallel research agents. Milestones organized by week with P0/P1/P2 priorities, task owners, and exit criteria. Includes immediate pre-week-1 actions, minimum viable submission definition, and risk watchlist. (Files: `01_Research/2026-08-09_initial_research/MILESTONES.md`, `01_Research/2026-08-09_initial_research/SUMMARY.md`, `01_Research/README.md`)

### 2026-08-14

- **[AI (via Adam)]** Initialized the project as a git repo and pushed to GitHub (`Adamex-SVK/yt-diag`, private), completing the MILESTONES.md pre-Week-1 task "Create GitHub repo for code." Added `.gitignore` scoped for large data/model artifacts under `02_Data`, `03_Models`, `04_Experiments`. (Files: `.gitignore`)
- **[AI (via Adam)]** Created `compute_scaling.md`: re-evaluates every hardware-driven constraint from the Aug 9–10 research (frozen backbones, 16–24 frames, 2,000-video cap, single-pass hyperparameter tuning) against the newly available A40 (48GB) + 96-core + 1.5TB RAM setup, plus guidance on when cloud GPU is actually worth renting. Recommends jumping to fine-tuned Tier 2/3 architecture (LoRA ModernBERT, DINOv2 ViT-B/L, TimeSformer, cross-attention fusion) on a larger CC-video dataset (5,000–8,000 if availability allows), and revises the "Excellent" AUC-ROC target confidence from Low to Medium — while keeping the external-noise ceiling (unaffected by compute) as the honest caveat. (Files: `01_Research/2026-08-14_scaling_and_features/compute_scaling.md`)
- **[AI (via Adam)]** Created `additional_features.md`: scopes Adam's proposed engineered features — frame color temperature, brightness/saturation, face presence and face-area ratio (MediaPipe Face Detection), and voice prosody (pauses, loudness, pitch, speaking-rate variability via openSMILE eGeMAPS) — into concrete features, recommended tools, and integration points (metadata baseline columns, optional 4th deep-model modality, a 5th ablation, and a cross-check against the Integrated Gradients attribution layer). Flagged as P1/P2, off the critical path, CPU-only work that runs in parallel with A40 training time. (Files: `01_Research/2026-08-14_scaling_and_features/additional_features.md`)
- **[AI (via Adam)]** Transferred the `wayfinder` and `setup-matt-pocock-skills` skills from the Kanlen project into this repo (`.agents/skills/`, symlinked into `.claude/skills/`), so long-horizon planning (mapping the whole project as decision tickets) can run here. Also symlinked the pre-existing `changelog` and `folder-summary` skills into `.claude/skills/` — they lived only in `.agents/skills/` and were never actually invocable, since this harness scans `.claude/skills/` for project skills. **Not yet run:** `setup-matt-pocock-skills` still needs to be run once (interactively) to scaffold `docs/agents/issue-tracker.md` before `wayfinder` can chart a map against the `Adamex-SVK/yt-diag` GitHub Issues tracker. (Files: `.agents/skills/wayfinder/`, `.agents/skills/setup-matt-pocock-skills/`, `.claude/skills/wayfinder`, `.claude/skills/setup-matt-pocock-skills`, `.claude/skills/changelog`, `.claude/skills/folder-summary`)
- **[AI (via Adam)]** Charted the `/wayfinder` map for locking YT-Diag's implementation plan (GitHub issue #1 on `Adamex-SVK/yt-diag`, label `wayfinder:map`), against the GitHub tracker doc's conventions directly (`setup-matt-pocock-skills` still not run). 8 child tickets created and wired with native GitHub sub-issue/blocked-by relationships, covering all 7 forks Adam listed plus one split out (evaluation-rigor scope). Reality check baked into the map: as of 2026-08-14 (day 4 of the Aug 11–17 Week 1 window), zero pre-Week-1/Week-1 `MILESTONES.md` tasks were actually done. (Files: none locally — map and tickets live entirely as GitHub issues)
- **[AI (via Adam)]** Resolved the environment half of wayfinder ticket #2: created `.venv`, installed `yt-dlp==2026.7.4` (`requirements.txt`), installed `ffmpeg` via Homebrew, smoke-tested metadata/license-field/thumbnail/auto-subs extraction against 3 real videos — all confirmed working. Provisioned Adam's YouTube Data API v3 key into a gitignored `.env` (not committed). (Files: `requirements.txt`, `.venv/` (gitignored), `.env` (gitignored))
- **[AI (via Adam)]** Ran the CC-license availability scan (real `search.list` pagination, not the `totalResults` estimate) across all category candidates, including all 3 "product reviews" mapping options, resolving the empirical half of ticket #2. Two findings: (1) `totalResults` overstates real availability by 30–1,000,000x and cannot be used for sizing decisions; (2) `videoCategoryId=22` (vlogs) and `=24` (Entertainment) combined with `videoLicense=creativeCommon` reproducibly return zero results — a `search.list` filter quirk, not real scarcity — confirmed via debug trace and fixed with a keyword-assisted workaround (`q=vlog`, `q=product review`) that recovers hundreds of results. Real CC-video counts via bare category+license filtering are much lower (~95–110/category) than the original `compute_scaling.md` 5,000–8,000 scaling ambition assumed. Findings feed tickets #3 (dataset size) and #4 (category mapping), both now unblocked. (Files: `02_Data/cc_availability_scan.py`, `02_Data/cc_availability_scan_results.json`, `02_Data/cc_availability_scan_findings.md`, `02_Data/README.md`)
- **[Adam, via AI]** Decided to drop the CC-only licensing constraint for frame sampling and audio-derived features (voice-modulation prosody, Whisper fallback transcription), accepting that downloading non-CC video/audio for those two components falls outside YouTube's TOS. Explicitly Adam's call, made after the tradeoff was raised directly (metadata/thumbnails/captions were never license-restricted; only the media-download step for frames/audio was). Why: removes the CC-scarcity bottleneck (~95–110 videos/category found in the scan) entirely, since supply of non-CC public video is effectively unlimited — the dataset-size constraint shifts to collection/processing throughput within the remaining calendar time, not video availability. Does not resolve the separate `videoCategoryId=22/24` API quirk (ticket #4), which is independent of license. (Files: `02_Data/cc_availability_scan_findings.md` — addendum)

### 2026-08-18

- **[AI (via Adam)]** Built `collect_and_extract.py`, the end-to-end per-video collection/feature-extraction pipeline (data_retrieval.md §Priority 4): discovers video IDs per category via `search.list` (no license filter, per the 08-14 CC-constraint-drop decision), then per video uses `yt-dlp` for metadata + thumbnail + best-effort auto-subs, downloads the video via `yt-dlp`, extracts `N` uniformly-timestamped frames via `ffmpeg`/`ffprobe`, and deletes the video file — only derived features are kept on disk. Idempotent/resumable via a `.done` marker per video and a `collection_manifest.csv` progress log, so it can be copied to a second machine (school computer) and re-run safely after a crash; only requires `yt-dlp`/`ffmpeg`/`ffprobe` on PATH plus the existing `.env` API key. Smoke-tested end-to-end on a real video (`jNQXAC9IVRw`) — metadata/thumbnail/5 frames produced correctly, temp video file deleted, manifest updated; test output then deleted so no placeholder data seeds the real dataset. Not yet run for real collection: category→`videoCategoryId` mapping (ticket #4) and per-category target count (ticket #3) are still open, so the script's `CATEGORIES` dict and `--target` are placeholders pending those decisions. (Files: `02_Data/collect_and_extract.py`, `02_Data/README.md`)
- **[AI (via Adam)]** Reconciled `collect_and_extract.py` with the actual locked decisions from the "YT-Diag Architecture Digest" artifact (2026-08-14, previously only in an ephemeral session, not written to any file in this repo) and `additional_features.md`, then extended the pipeline to match. Why: the initial version sampled frames uniformly, but the locked architecture specifies 16–24 frames **denser in the first 60s** (engagement signal concentrates early) — a real mismatch, not a style choice. Changes: (1) replaced uniform frame sampling with `compute_frame_timestamps()` — 60% of frames in the first 60s, remainder spread across the rest, verified against a synthetic 600s duration (12/20 frames land ≤60s as intended); (2) added visual engineered features per `additional_features.md` §1 (color temperature via McCamy's approximation, brightness/saturation/contrast, face presence/count/area/centrality) computed on the thumbnail + every sampled frame before deletion; (3) added audio/prosodic features per §2 — eGeMAPS (88-feature openSMILE set) plus explicit VAD-based pause-length stats (`webrtcvad`) — extracted from the video's audio track (required switching `download_video()`'s format selector from `bestvideo`-only to `bestvideo+bestaudio`, since video-only streams have no audio to extract) before that audio file is also deleted. All three additions are optional-dependency-guarded (fall back to a warning + skip, not a crash, if a dep is missing) so a partial environment on the school computer degrades gracefully instead of blocking collection entirely. Found and fixed two real dependency pitfalls during smoke-testing: `opencv-python-headless>=5` silently drops `CascadeClassifier` (pinned `<5`); `mediapipe==1.0.1`'s current PyPI release removed the `solutions` API this was written against (left mediapipe out of `requirements.txt`, hardened the face-detector's fallback to catch `AttributeError` too, not just `ImportError`). Re-verified end-to-end 3× against the exact dependency set now pinned in `requirements.txt`, on a clean venv state, before considering it done. (Files: `02_Data/collect_and_extract.py`, `02_Data/README.md`, `requirements.txt`)

### 2026-08-20

- **[AI (via Adam)]** Closed every remaining feature-completeness gap identified when asked "are we clear on all the features we're pulling, for the models" — the metadata baseline was missing 3 of 12 documented columns plus the `definition` field, and the transcript pipeline had no fallback for videos with missing/poor auto-captions. Changes to `collect_and_extract.py`: (1) `fetch_metadata()` now captures `definition` (HD/SD) and derives `title_length`/`description_length`/`tag_count` at collection time, so `metadata.json` alone is a complete baseline row instead of needing a later derivation pass; (2) added the full auto-captions→Whisper fallback pipeline from `data_retrieval.md` §4.3 — `caption_quality_ok()` implements the documented heuristic (word count > 50, ≤20% `[Music]`/`[Applause]`-style tag lines), and `transcribe_with_whisper()` (lazy-loaded `small.en`, reused across the run) covers everything that fails it; every video now gets `transcript.txt` + `transcript_info.json` (source + whether auto-captions existed) regardless of which path produced it. Also built `compute_labels.py` — the CLAUDE.md label (top-quartile views-per-day-since-upload, normalized by subscriber count, within category) — deliberately kept separate from the collection script since a label is a whole-cohort ranking, not a per-video computation. Writes `label.json` per video + `labels_summary.csv` per category. **Verified all of this together, not just individually**, on a real 2-video test batch (`jNQXAC9IVRw`, `9bZkp7q19f0`) run through the full pipeline end-to-end, which caught two further real bugs neither unit test would have: `download_video()`'s format selector needed `bestaudio` (already fixed 08-18, re-confirmed here); and `yt-dlp==2026.7.4` (the version pinned since 08-14) fails all video downloads outright against YouTube's current anti-bot JS challenge (`HTTP Error 403`) — fixed by upgrading the pin to `yt-dlp==2026.8.19`, adding `deno` as a hard dependency (`check_dependencies()`, install via `brew install deno`), and passing `--remote-components ejs:github` on every yt-dlp call. Also added `relevanceLanguage=en` to discovery after the Gangnam Style test video (no English auto-captions) caused Whisper's English-only `small.en` model to produce a garbage transcript — matches the mitigation `data_retrieval.md` §2.3 already specified. Test output deleted after verification; nothing collected for real yet (still pending tickets #3/#4). (Files: `02_Data/collect_and_extract.py`, `02_Data/compute_labels.py`, `02_Data/README.md`, `requirements.txt`)
- **[Adam, via AI]** Closed wayfinder tickets #3 and #4, both blocking real collection. #4 (product-reviews category mapping): keeps the Entertainment (categoryId=24) + `q="product review"` keyword workaround already implemented as the script's default — justified by the scan numbers in `cc_availability_scan_findings.md` (bare categoryId=24 returns 0 due to a `search.list` filter quirk; the keyword-assisted version returned 410 retrievable vs. Science & Tech's 107, the only candidate with enough volume at the new 2,000/category target). #3 (dataset size): 8,000 total, uniform 2,000/category across all 4 categories — the upper end of `compute_scaling.md` §2's 5,000-8,000 scaling recommendation, justified by the CC-only constraint (the scan's actual scarcity bottleneck) already having been dropped 2026-08-14, so non-CC supply is no longer the limiting factor. `collect_and_extract.py`'s `--target` default changed from the 50-per-category placeholder to `DEFAULT_TARGET_PER_CATEGORY = 2000`; the "pending ticket #4" hedging comments removed since CATEGORIES is now the locked answer, not a placeholder. Both tickets closed on GitHub with the decision and reasoning recorded in the closing comment. (Files: `02_Data/collect_and_extract.py`, `02_Data/README.md`, GitHub issues #3, #4)

### 2026-08-22

- **[AI (via Adam)]** Fixed four real bugs surfaced by an actual pilot run on the school computer (Windows), each confirmed against live evidence, not guessed: (1) the bare-`videoCategoryId`-returns-0 API quirk spread from categoryId 22/24 to comedy(23)/howto(26) too — confirmed directly against the live API before shipping the fix (every category now carries a keyword); (2) every `open(..., "w")` in `collect_and_extract.py` now passes `encoding="utf-8"` explicitly — Windows defaults text writes to the system codepage, which crashed on real videos containing Arabic text and an ₹ symbol (macOS/Linux default to UTF-8, masking this locally); (3) `--cookies-from-browser chrome` doesn't work on current Chrome (App-Bound Encryption breaks external DB decryption, yt-dlp issue #10927) — added a `--cookies PATH` flag taking an exported `cookies.txt` instead, which sidesteps the issue entirely; (4) the pipeline was single-threaded and averaged ~50s/video on the school computer, extrapolating to ~5 days for the full 8,000-video run — added `--workers` (default 3, `ThreadPoolExecutor`) with staggered submission (`--pacing` divided across workers, so concurrency doesn't reintroduce the bot-check burst pattern already fixed on 08-20) and a `_whisper_lock` serializing the one shared Whisper model instance across threads (loading a copy per worker would cost too much RAM). Verified the concurrency fix correctness (not just "didn't crash") on a real 3-video concurrent run — confirmed genuine overlap (two fast videos finished while a third kept running) and no output corruption across threads' file writes. (Files: `02_Data/collect_and_extract.py`, `02_Data/README.md`)
- **[AI (via Adam)]** Fixed a real DLL race exposed by `--workers 3` on the school computer: the first `[WinError 1114] c10.dll` failure (08-22, earlier today) looked like a one-off environmental fluke, but a `--workers 3` pilot run hit it in 3/20 videos (15%) — clearly not random. Root cause: `transcribe_with_whisper()`'s `import whisper` statement (which triggers torch's native DLL loading) sat outside `_whisper_lock`, so concurrent threads' first Whisper call could race on Windows' DLL loader simultaneously. Fixed by moving the import inside the lock, serializing the whole first-import-and-load sequence across threads. Re-verified no regression/deadlock on a real 2-video concurrent run on this machine before shipping (can't reproduce the Windows-specific race itself locally, but confirmed the fix doesn't break normal operation). (Files: `02_Data/collect_and_extract.py`, `02_Data/README.md`)

- **[AI (via Adam)]** The lock-based Whisper fix above was NOT sufficient -- re-tested at `--workers 8` on the actual server (Dell PowerEdge R740, dual Xeon Gold 6240R, 96 threads, 1.5TB RAM -- discovered this run is on the same class of machine `compute_scaling.md` described for training) and the c10.dll failure rate rose to ~25-35%, scaling *with* thread count despite the import already being serialized behind `_whisper_lock`. That rules out a simple import race. Real cause (Windows-specific, well documented): native DLLs using thread-local storage -- torch's C10/MKL among them -- must have Windows retroactively initialize TLS for every thread already alive in the process at `LoadLibrary` time, with a hard limit; loading torch for the first time while N worker threads already exist can fail from thread *count* alone. Fix: `preload_whisper()` now loads the model once in `main()`, single-threaded, before the `ThreadPoolExecutor` is created, so torch's DLLs are already resident before any worker thread exists. `transcribe_with_whisper()` simplified to just the inference call under the lock. Verified locally that the preload log line now appears before any per-video processing starts and normal operation is unaffected; the Windows TLS race itself can't be reproduced on macOS, so this still needs confirmation at `--workers 8`+ on the real server. (Files: `02_Data/collect_and_extract.py`, `02_Data/README.md`)

### 2026-08-26

- **[Emmanuel]** Built `compute_labels_v2.py`, a revised label computation superseding `compute_labels.py` (v1), after review of commit cd569ee found two statistical flaws in the v1 label (both confirmed by independent review): (1) views-per-day-since-upload assumes linear view accumulation, but YouTube views are front-loaded — the score decays with video age, so the label partly encodes "how new is this video" and age-correlated metadata features could predict it without learning content; (2) normalizing by CURRENT subscriber count is post-outcome (reverse causality: a viral video grows its own denominator), subs is also one of the 12 metadata model features (same post-outcome number in label denominator and model input = target-leakage shortcut), and v1's subs=1 fallback for hidden counts near-deterministically mislabeled those videos as viral. Also corrected a citation error found during the review: Wu et al. (2018)'s "relative engagement" is rank-percentile of average WATCH PERCENTAGE calibrated against video DURATION at a fixed 30-day horizon — v1's formula was never that metric and must not be cited as "following Wu et al."; v2 borrows only the rank-within-strata calibration method ("inspired by"). v2 design — stratify instead of divide: `log(1+views)` ranked within (category × age-band × channel-size-band) cells, top quartile = viral, equal-count quantile bands (default 4×4) per category, subs used only as a coarse stratifier (post-viral sub growth only mislabels if the channel crossed a band boundary — rare and disclosable — vs. continuously rescaling every score); every cell has ~25% positives by construction, so category/age/channel-size are marginally uninformative about the label (leakage shortcut removed by design) and the proposal's own "similarly sized channels, same category" definition is implemented literally. Degenerate inputs are excluded and recorded in `labels_excluded.csv` (videos <30 days old at collection, missing dates/view counts, hidden/missing subs), never imputed. Verified on a 400-video synthetic cohort with planted confounds (views correlated with both subs and age) and all exclusion cases: viral rate flat at ~27–28% across every age and size band (vs. 0%→81% across size bands for a naive raw-views quartile — the exact artifact stratification removes), all 11 exclusions caught with reasons, byte-identical output across runs. A `fetch_channel_stats.py` draft (channel-median-views normalizer) was created and deleted the same session — the channel-median approach was rejected in discussion because snapshot data cannot give the channel's PRIOR uploads at the SAME view-horizon (it would mix future uploads and unequal accumulation windows). **Pending Adam's sign-off, since this revises the label definition locked in CLAUDE.md** — v1 kept as reference until then; CLAUDE.md's label wording deliberately NOT updated yet. Band counts and --min-age-days should be revisited after EDA on the real collected age/subscriber distributions. (Files: `02_Data/compute_labels_v2.py`, `02_Data/README.md`)
- **[Emmanuel]** Fixed three defects in `compute_labels_v2.py` found by independent review before sign-off, and softened its overstated claims. (1) Quartile off-by-one: `percentile >= 0.75` included the boundary video, over-labeling every cell (26/100, 6/20 — the source of the earlier ~27–28% synthetic viral rate); replaced with exactly the top ⌊n·0.25⌋ videos per cell (min 1). (2) Tie-splitting across bands: band assignment was rank-based with a `video_id` tiebreak, so videos with identical subscriber counts (common — the API rounds subs to ~3 significant figures, and same-channel videos always tie) could land in different "channel-size" bands; bands are now assigned by value (all equal values share the first occurrence's band), verified by planting 52 identical-subs videos that a rank split would have straddled. (3) Exact view-count ties at a cell's viral cutoff were silently resolved by `video_id`; now still deterministic but counted, printed as a per-category warning, and flagged per-video in a new `warnings` column of `labels_summary.csv` and `label.json`. Claim corrections in the docstring/README: stratification *reduces direct shortcuts* from the peer-group variables but does not force models to learn content (they still see exact age/subs values — residual within-band gradients remain); the coarse-stratifier defense against post-outcome subs *reduces, does not eliminate,* reverse causality (band-crossing rate to be quantified on real data via a halved-subs sensitivity check); labels are a retrospective cohort-relative ranking computed before any split (transductive — to be disclosed in the report, with train-only band edges as a planned sensitivity analysis). Re-verified on the extended synthetic cohort (452 videos + tie cases): viral rate ≤25% and flat across all bands, identical-subs videos in one band, cutoff-tie path exercised on a forced 3-way boundary tie, deterministic output. (Files: `02_Data/compute_labels_v2.py`, `02_Data/README.md`)
- **[Emmanuel]** Created `02_Data/FEATURES.md`, the project's feature dictionary — one consolidated reference for every feature: definition, source field, and role (model input vs label-only vs label-v2 stratifier vs identifier). Why: feature information was scattered across `data_retrieval.md` §5.4, `additional_features.md`, `02_Data/README.md`, and the collection script itself, and the sources had drifted apart in ways nobody had written down: (1) three of the documented "12 metadata features" (view/like/comment counts) are label-only and must never be model inputs, so the real model-facing metadata list is 9 columns plus planned schedule features; (2) "upload date/time" is only half-captured — `metadata.json` stores the date, not the time, so publish-hour features need a `videos.list snippet.publishedAt` backfill (~160 batched calls for 8,000 videos); (3) "caption availability" (feature #12) isn't in `metadata.json` at all — it lives in `transcript_info.json` as `had_auto_captions`. Sections 1–4 document Adam's collected features (structured metadata, visual engineered, audio/prosodic, raw modalities) with the exact field names the pipeline writes; section 5 documents the v2 label fields; section 6 documents newly planned features (publish datetime backfill, weekday/weekend, time-of-day bucket with channel-country timezone conversion, channel country, and the 30-day prospective tracking cohort with daily view/subscriber snapshots). Decisions recorded there: no US-only or country filtering (channel country is self-declared and often missing — record it, convert timezones where known, mark time-of-day missing otherwise); day-of-week preferred over hour buckets for timezone robustness. Incorporates independent-review corrections: UTC hour stored as sin/cos cyclical encoding; the tracking cohort is a fixed capped panel (~2,000–4,000 videos, with continuous `views_at_7d`/`views_at_30d` outcomes, day 7 preliminary only) rather than everything discovery returns; first channel observation named `subscriber_count_at_first_observation` (with observation age and `hiddenSubscriberCount`), not "day-0 subs", since even hours-old observations can include video-driven gains and public counts are rounded; channel-panel upload checks cost ~1 call per channel per poll (stagger by upload frequency); day-1-views features belong to a separate early-warning model, never the upload-time model; schedule-feature importance to be worded as association, not causation. (Files: `02_Data/FEATURES.md`, `02_Data/README.md`)
- **[Emmanuel]** Built the two scripts that implement FEATURES.md §6. (1) `track_new_videos.py` — the prospective 30-day tracker: one idempotent daily "tick" that discovers newborn videos per category via bounded `publishedAfter/publishedBefore` windows (`order=date` only — `order=viewCount` deliberately excluded from the main cohort since it selects on an early version of the outcome; recorded sampling provenance per video: `sampling_arm`, `window_start/end_utc`, `search_rank`; 2h window overlap + global dedup to catch boundary-inclusive bounds and delayed indexing), holds a fixed capped cohort (default 3,000 ≈ FEATURES.md's 2,000–4,000 panel, per-category caps), then snapshots views/likes/comments (`videos.list`, batched 50) and channel subs/`hiddenSubscriberCount`/country (`channels.list`) for every cohort video ≤30 days old. Every row carries exact `observed_at_utc`/`age_hours`, so scheduling is external and fault-tolerant by design: late/missed runs leave gaps (not corruption), doubled runs are suppressed by a min-gap check, deleted videos are recorded as `missing` (attrition is an outcome), aged-out videos stop being polled. State is three CSVs in gitignored `02_Data/tracking/`. ~100 cheap API calls/day at full cohort; intended to run under an OS scheduler (Windows Task Scheduler/cron — NOT a sleep loop, NOT a cloud function: no infra, state stays next to the repo) with a dedicated API key so tracking never competes with collection quota. (2) `backfill_published_at.py` — one-shot, resumable backfill of what `metadata.json` never captured: full `published_at_utc` (publish-time features need it; `upload_date` is date-only), `caption_available` (feature #12), `youtube_category_id`, `channel_country`, `hidden_subscriber_count`; writes a separate `metadata_extra.json` per video dir so it can never race or corrupt the collector's own files while Adam's run finishes. Both scripts verified against a stubbed API (tracker: 3-tick simulation covering dedup, caps, min-gap, age-out, deleted-video handling; backfill: field correctness, missing-video marking, second-run-finds-zero resumability). Neither has run for real yet — the tracker needs a key in `.env` and a daily scheduler, and every unstarted day is a lost day of cohort. (Files: `02_Data/track_new_videos.py`, `02_Data/backfill_published_at.py`, `02_Data/README.md`, `02_Data/FEATURES.md`, `.gitignore`)
- **[Emmanuel]** First real tracker tick ran 2026-08-26 ~18:00 UTC on a dedicated API key (gitignored `.env`), starting the prospective cohort clock: 789 videos discovered in the 24h window (comedy 241, howto 240, vlogs 243, product_reviews 65 — product reviews are genuinely scarcer at `order=date`, matching the retrospective scan's pattern), all 789 snapshotted (age at first observation 0.2–24h, median 10.1h; views present for 100%, like counts hidden by creators on 137), 735 unique channels snapshotted (channel country present for 73% — better than feared for the timezone conversion; 0 hidden subscriber counts; median subs 12.1k, max 37.4M). At this rate comedy/howto/vlogs hit their 750/category caps in ~3 days; product_reviews fills in ~11. Cohort state in gitignored `02_Data/tracking/` — copy it (3 CSVs + `discovery_state.json`) alongside the script when moving daily ticks to the TUM server's Task Scheduler. (Files: none versioned — `02_Data/tracking/` data only)
- **[Emmanuel]** Extended the tracker with thumbnail capture and per-video category/title recording, then ran a same-day capture pass over the live cohort. Why thumbnails: creators change thumbnails (and titles) post-upload as an optimization tactic — the retrospective dataset can only ever see the *current* thumbnail, so the prospective cohort is the one chance to preserve the at-publish version and record every change event (itself a candidate feature: "creator swapped the thumbnail after a weak first day"). Implementation: each snapshot already fetches `snippet`, so the thumbnail URL (best available quality), `categoryId` (authoritative per-video category — the official YouTube taxonomy has no "product reviews", ours remains the Entertainment+keyword proxy), and current `title` cost zero extra API quota; thumbnails are CDN GETs, hashed (sha256), and a new image file is stored only when the hash differs from the last stored version, with every check logged in `thumbnail_snapshots.csv`. Added a CSV schema-migration helper (`ensure_fields`) so the already-running cohort's `video_snapshots.csv` gained the new `youtube_category_id`/`title` columns without losing day-1 rows. Verified on the stubbed-API suite (thumbnail swap detected and stored as `_v1`, unchanged thumbnails not re-stored, migration keeps old rows aligned), then ran live: all 789 thumbnails captured (94MB, 780 maxres 1280×720 — the vision backbone's native resolution — 9 standard, 0 failures), per-video categories verified to match the discovery filters exactly (23/26/22/24), all titles recorded. (Files: `02_Data/track_new_videos.py`, `02_Data/README.md`, `02_Data/FEATURES.md`; data in gitignored `02_Data/tracking/`)
- **[Emmanuel]** Found and fixed critical Shorts contamination in the tracker cohort, spotted by eyeballing the day-1 thumbnails: a duration audit (durations were never captured — `contentDetails` was not in the snapshot `part`) showed **94% of the 789 day-1 videos were ≤180s** (Shorts ceiling; comedy 100%, howto 90%, vlogs 92%, product_reviews 91%) — at `order=date`, fresh uploads are overwhelmingly Shorts, which have different distribution surfaces and virality dynamics than the videos this project models. Fix, three parts: (1) discovery now passes `search.list`'s server-side `videoDuration=medium`/`long` filters (≥4min) per category; (2) every candidate's authoritative `contentDetails` duration is fetched (batched, 1 unit/50) and verified before admission — `duration_sec` is now a cohort column, and the check caught 20 sub-4min videos that slipped past the search filter in the very first run; (3) the 742 day-1 sub-4min videos were retagged to a `short_form` sampling arm — still snapshotted daily (their growth curves are a free Shorts-vs-regular comparison dataset at trivial quota cost) but excluded from the per-category caps so they can never crowd out the real panel. Effect was dramatic: the unfiltered day-1 run yielded 47 usable (≥4min) videos from a 24h window; the first filtered run admitted **692 in a ~5h window** (Shorts had been crowding every search page). Main arm now 739 videos (median duration 16.5min; comedy 197, howto 191, vlogs 342, product_reviews 9 — the product-reviews scarcity persists in prospective discovery too). **Implication flagged for Adam: the retrospective `collect_and_extract.py` discovery has no duration filter either** — his collected `metadata.json` files have `duration`, so the contamination rate is auditable post-hoc, but the 8,000-video dataset may need the same `videoDuration` treatment and/or post-hoc filtering, which affects effective dataset size. Verified via the extended stub suite (Shorts rejected at admission with `videoDuration` asserted on every search call) before the live run. (Files: `02_Data/track_new_videos.py`, `02_Data/README.md`, `02_Data/FEATURES.md`; data in gitignored `02_Data/tracking/`)
- **[Emmanuel]** Two scarcity measures for the review categories, both live from day 1 of the cohort. (1) product_reviews now searches three query arms (`product review`, `unboxing`, `first impressions`) instead of one — at `q="product review"` alone it admitted 9 main-arm videos/day, which would have left its 750 cap ~2/3 empty when the 30-day window closes; the very first multi-arm window admitted 98 more (breakdown: `unboxing` 95, `product review` 4, `first impressions` 3 — unboxing is the workhorse), with each video's exact source query recorded in `discovery_source` so the slices stay auditable. (2) Added a fifth BACKUP category `tech_reviews` (Science & Technology, categoryId 28 — one of the original ticket-#4 candidates for the product-reviews mapping; queries `review`/`unboxing`): it accumulates alongside the main four as insurance so that if product_reviews stays too thin or noisy, the team can swap it in *with cohort history already built* instead of starting from zero on swap day — the swap itself is a team decision with Adam, and until then tech_reviews stays out of the main analysis. Caps now divide `--max-cohort` by the 4 MAIN categories only, so the backup's own 750 cap doesn't shrink theirs; `--discover-pages` default dropped 5→3 (8 query arms × 2 duration filters ≈ 48 search calls/tick worst case — schedule the second daily tick with `--no-discover` to spend search quota once a day). Main arm after the first multi-arm tick: comedy 235, howto 233, vlogs 442, product_reviews 107, tech_reviews 45. (Files: `02_Data/track_new_videos.py`, `02_Data/README.md`, `02_Data/FEATURES.md`; data in gitignored `02_Data/tracking/`)

### 2026-08-27

- **[Emmanuel]** Fixed all 9 findings from the automated review on PR #14, two of them real data-loss bugs. (1) **The age-out check guaranteed the 30-day outcome was never observed**: a video's last snapshot landed at day ~29.x and its next due tick fell past the horizon and was skipped — `views_at_30d` would have required extrapolation on every single video. Now each video past the horizon stays eligible for exactly one terminal at/after-horizon sample, bounded by a 3-day grace window so long-dead videos don't resurrect after scheduler downtime. (2) **The discovery checkpoint advanced even when search/duration API calls failed**, permanently dropping the failed window's videos (the 2h lookback wouldn't re-cover it); the checkpoint now freezes on any failed call and the next tick re-covers the whole window (global dedup makes re-coverage free). (3) Cap admission was loop-order-biased (earlier query arms and medium-duration videos always won near the cap); candidates are now admitted newest-first across all arms, matching the order=date frame. (4) A transient `channels.list` failure in the backfill became permanent missing country data (the output file was written anyway and re-runs skipped it as done); affected videos are now held back as pending so re-runs retry them. (5–9) Documentation drift: the tracker docstring's quota estimate (said ~4–8 search calls/tick, actually up to 48 — nearly half the 100/day search budget, reinforcing the run-discovery-once-daily advice) and state inventory (said 3 CSVs; actually 4 CSVs + `discovery_state.json` + `thumbnails/` — undercounting risked losing thumbnail history in the server move); FEATURES.md's stratifier role guidance still called stratifier model-inputs "safe" (contradicting the corrected v2 claims — now points to the §5 limitations note); FEATURES.md described `short_form` as an ongoing arm when it is only the retagged day-1 cohort (ongoing discovery rejects new sub-4min candidates); README tracker row said "not yet running" and "3 CSVs". Test suite extended to lock in the fixes: terminal-sample case (video at 30.5d with a day-29 snapshot gets exactly one more sample at ≥720h; a 35d video beyond grace does not), and a checkpoint-freeze case (failed searches leave `discovery_state.json` byte-identical) — the checkpoint test caught its own first version being wrong (all-categories-at-cap makes zero search calls, which legitimately advances the checkpoint). (Files: `02_Data/track_new_videos.py`, `02_Data/backfill_published_at.py`, `02_Data/README.md`, `02_Data/FEATURES.md`)
- **[Emmanuel]** Second review round on PR #14, all 3 findings fixed: (1) a **process-level tick lock** (`tracking/tick.lock`, atomic `O_EXCL` creation, works on Windows and Unix) — the min-gap check only made *sequential* reruns idempotent; two overlapping invocations (scheduled task + manual run — exactly the situation during the server handoff week) would both read the same state, both consider everything due, and concurrently append duplicate rows and overwrite discovery/thumbnail state. A second invocation now exits cleanly without touching state; locks older than 2h are treated as a crashed tick's leftover and taken over; the lock is released in a `finally` so a failed tick never wedges the scheduler. (2) The discovery checkpoint is written via **tmp-file + atomic `os.replace`** — an interruption mid-write could have left invalid JSON that crashed every subsequent tick at startup, exactly the failure mode an unattended scheduled job can't recover from. (3) README's tracker row still claimed new sub-4min videos join the `short_form` arm — corrected to match the code and FEATURES.md (ongoing discovery rejects them; `short_form` is only the retagged day-1 cohort). Lock behavior added to the stub test suite: exclusive while held, stale takeover after age threshold, clean reacquire after release. (Files: `02_Data/track_new_videos.py`, `02_Data/README.md`)
- **[Emmanuel]** Third review round on PR #14, all findings fixed. (1) The stale-lock takeover was unsound on its own: a 2h mtime threshold doesn't prove the owner exited (worst-case serial thumbnail downloads with 30s timeouts could keep a live tick busy longer), and `release_lock` removed whatever lock existed — so a slow-but-alive tick could lose its lock to a newcomer and then delete the newcomer's lock on exit. Now: an **ownership token** (pid + random) is written into the lock and release only removes a lock whose token matches, and the running tick **heartbeats** the lock (refreshes mtime inside `api_get` and per thumbnail download), so an old mtime genuinely means "no progress for 2h" — dead or wedged — not merely slow. Token-guarded release added to the test suite. (2) Corrected "at-publish thumbnail" claims in the tracker comments, FEATURES.md, and README to **"first-observed (near-publish)"**: discovery can lag publication by up to ~24h, so pre-discovery thumbnail/title changes are invisible — a limitation to disclose in the report's methodology, not to paper over in docs. (3) Updated PR #14's description to disclose that the branch now carries three rounds of post-review fixes beyond the originally-reviewed #12/#13 content, so reviewers don't rely on the stale "nothing new" premise. (4) Follow-up wording fix: the tracker docstring claimed `views_at_7d`/`views_at_30d` "can be read off exactly" — daily samples rarely land at exactly 168/720h, so they are *interpolated* from the observations bracketing each horizon (the terminal sample guarantees day 30 is bracketed, never extrapolated); docstring now says so. (Files: `02_Data/track_new_videos.py`, `02_Data/FEATURES.md`, `02_Data/README.md`)
- **[Emmanuel]** Raised the tracker cohort cap 3,000 → 12,000 (3,000/category incl. the backup) and parallelized thumbnail downloads to make that practical. Rationale for the raise: API quota is use-it-or-lose-it and was never the binding constraint (snapshots at a mature 12k cohort ≈ 940 one-unit calls/day, ~10% of budget; search spend unchanged at ~48 calls/day since discovery volume doesn't depend on the cap), storage/RAM are available, and a larger clean panel lets the post-deadline extension *select* its multimodal-extraction subset (e.g. matched viral/underperformer pairs) instead of taking whatever a small pool offers — the cap remains a deliberate bound, not an unbounded sweep, and scarce categories (product_reviews, tech_reviews) are supply-limited well below it regardless; the raise mainly deepens comedy/howto/vlogs. The actual binding constraint was tick wall-time: serial thumbnail downloads (~0.3s each) would have meant hour-long ticks at 12k. Downloads now run on an 8-worker thread pool — only the network fetch is parallel; hashing, image writes, and CSV appends stay single-threaded in submission order, so output remains deterministic and the existing lock/heartbeat semantics are untouched (each worker heartbeats). Full stub suite re-run green after the change. Scheduling and state ownership note: the tracker now runs on Emmanuel's MacBook via two launchd agents (08:00 full tick, 20:00 `--no-discover`), chosen after TUM pool PCs refused "run whether logged on or not" (domain policy denies the batch-logon right) and with Adam's always-on server unavailable for setup; launchd runs missed jobs on wake, so daily lid-opening suffices, and the never-run TUM Task Scheduler entries are to be deleted. (Files: `02_Data/track_new_videos.py`, `02_Data/README.md`, `02_Data/FEATURES.md`)
- **[Emmanuel]** Post-merge review corrections to the cap raise (PR #15's review arrived after merge; the previous entry's "12,000 (3,000/category incl. the backup)" was self-contradictory). (1) **Honest capacity accounting**: `--max-cohort` (12,000) is divided by the 4 MAIN categories (3,000 each) and the backup category gets the same cap ON TOP, so the true main-arm ceiling is **15,000**, not 12,000 — docs and quota estimates now state this everywhere (snapshots at the mature ceiling ≈ 1,200 one-unit calls/day ≈ 12% of budget, not the earlier ~940/"~100"); the code is unchanged since backup-on-top was the design intent, the documentation was wrong. (2) **Memory fix**: thumbnail results were materialized with `list(pool.map(...))`, holding every downloaded image until all finished — at 15k maxres blobs a ~2GB spike; the iterator is now consumed lazily inside the pool context so each blob is hashed and released as it arrives (order, and therefore CSV determinism, preserved). (3) Cleared remaining stale README facts: backup cap 750→3,000, snapshot quota estimate, and scheduler status (launchd agents on the MacBook are configured and running, not "awaiting a move" — though moving to an always-on server remains preferable if one appears). Full stub suite green. (Files: `02_Data/track_new_videos.py`, `02_Data/README.md`, `02_Data/FEATURES.md`)
- **[Emmanuel]** Second review round on PR #16, plus a repo-wide consistency sweep (process change after four consecutive review rounds each caught stale duplicates of facts fixed only where the reviewer pointed: from now on every changed fact is grepped across all scripts/docs and the full diff is read before pushing). (1) **Truly bounded thumbnail streaming**: `pool.map` submits every job eagerly and retains out-of-order completions (whole image blobs) until earlier jobs are yielded, so the previous "lazy iterator" fix was not actually bounded — one slow early download could still pile up hundreds of images. Replaced with a fixed-size FIFO of futures (at most 2×`THUMB_WORKERS` results alive), input order preserved; new test runs 60 jobs with randomized completion timing and asserts CSV order equals job order with peak concurrency ≤ 8. (2) Stale facts swept from the module: the docstring's "~100 one-unit calls/day at full cohort" → ~1,200 at the 15,000 ceiling; the CATEGORIES comment's "750 cap" → explicitly "then-750 (3,000 since 2026-08-27)"; a "three plain CSVs" section comment (not flagged, found by the sweep) → the real state inventory; the tick description still said `publishedAfter = now - 24h` and omitted duration filtering, verified admission, terminal samples, title/category capture, thumbnails, and launchd — rewritten to match the implementation. (3) FEATURES.md status cell still said "needs a daily scheduler on an always-on machine" → scheduling active on the MacBook via launchd, server migration preferred if available. Post-sweep grep for every previously-stale value returns only the explained historical mention. Full stub suite green (9 checks). (Files: `02_Data/track_new_videos.py`, `02_Data/FEATURES.md`)
- **[Emmanuel]** Third review round on PR #16: the runtime progress log was the last place still carrying the obsolete accounting — `discover()` printed `cohort total N/12000`, where N counted every row (short_form included) against a denominator that is neither the main-arm ceiling (15,000) nor comparable to N. It now prints `main arm M/15000 (3000/category x 5 categories)` plus the short_form row count separately, and the tick-start line likewise splits rows into main arm vs short_form. Also rewrote the thumbnail-block comment, which still described `pool.map` + lazy consumption after the implementation had moved to the bounded FIFO — left as-is, a future maintainer could reintroduce `pool.map` believing it to be the bounded version; the comment now names the mechanism and explicitly warns against `pool.map`. Sweep: no other `max_cohort` denominators or stray `pool.map` references remain. Suite green. (Files: `02_Data/track_new_videos.py`)
- **[Emmanuel]** Tracker hosting moved from `~/Documents/GitHub/yt-diag` to `~/GitHub/yt-diag` after the first unattended launchd tick failed with `[Errno 1] Operation not permitted`: macOS privacy (TCC) blocks background processes from `~/Documents`/`~/Desktop`/`~/Downloads`, Terminal's interactive grant doesn't carry over, and two Full Disk Access grants to Xcode's Python (the `python3.9` binary, then its `Python.app` bundle) were not honored. Relocation is the deterministic fix — no permissions, immune to Xcode re-signing. Both agents repointed and reloaded; the first unattended tick at the new path completed (622 videos discovered, all thumbnails captured, `tick done`). That tick also exposed a scheduling pitfall: YouTube's quota day resets at midnight Pacific (09:00 CEST), so the 08:00 CEST discovery run shared a quota day with the previous evening's five experimental discovery runs and hit `HTTP 429` on its last 8 search calls — the tracker correctly froze the checkpoint for re-coverage; a one-off re-run after the reset was scheduled, and the rule (one discovery run per quota day; manual ticks use `--no-discover`) is now in the README. (Files: `02_Data/README.md`; launchd agents and repo location are local to Emmanuel's machine, not versioned)
- **[Emmanuel]** Tracker schedule moved to 09:05 (discovery) / 21:05 (`--no-discover`) and the snapshot min-gap default lowered 12h → 8h, after Emmanuel asked whether shifting past the quota reset would avoid the 429s. It does — discovery at 09:05 CEST is the *first* run of the fresh Pacific quota day, so nothing run earlier can starve it — and checking the interaction exposed a second latent bug: the old 08:00/20:00 schedule was exactly 12h apart while the min-gap was also 12h, so the evening tick would have seen every video's last snapshot at ~08:0x, computed 11h5xm < 12h, and skipped nearly all snapshots (never observed only because yesterday's evening ticks were manual and closer together). The min-gap exists to make accidental doubled runs no-ops, not to enforce spacing; 8h still rejects any run within a work-shift of the last while letting two ticks 12h apart both snapshot everything. Both launchd agents now pass `--min-gap-hours 8` explicitly as well, so the behavior doesn't depend on which code revision is checked out. PR #17's README/changelog notes are folded into this change (its "08:00 is the day's one discovery run" rule was superseded before it merged) and #17 is closed. (Files: `02_Data/track_new_videos.py`, `02_Data/README.md`, `02_Data/FEATURES.md`; launchd agents local)
- **[Emmanuel]** Discovery page cap raised 3 → 5 per query arm × duration filter after Emmanuel asked why the 09:05 run admitted only 632 videos. Data answer: the run re-covered the same window the 08:27 run had already harvested (622 admitted before its 429s), so 632 was the genuine remainder — almost entirely from the arms whose calls had failed (vlogs 282, product_reviews 164, tech_reviews 94), i.e. re-coverage working as designed; and uniqueness is confirmed empirically (3,585 rows, 3,585 unique `video_id`s, 0 duplicates — dedup is global across the cohort and within-run across all arms/filters). But the analysis exposed a real coverage limit: the vlogs arm hit `search_rank` 150 on *both* duration filters (3 pages × 50 exhausted) and comedy came close (146/124). Because `order=date` returns newest-first, an exhausted cap under-samples the *earliest* hours of the window — the admitted videos' publish-hour histogram showed 7 at 22Z vs 118 at 01Z — a publish-hour bias that matters for the schedule features. At 5 pages the per-window capacity is 250 per arm/filter; worst-case search spend 8 × 2 × 5 = 80 calls/day (~40 in practice, scarce arms exhaust in 1–2 pages), under the 100-call budget with discovery once per quota day. The live AM agent also passes `--discover-pages 5` explicitly. The residual caveat (newest-first truncation for the busiest arms) is now documented in FEATURES.md §6 as an EDA check before using schedule features on this cohort. Quota figures swept everywhere (docstring, constants, README). (Files: `02_Data/track_new_videos.py`, `02_Data/README.md`, `02_Data/FEATURES.md`; launchd agent local)
- **[Emmanuel]** Refined the page cap into **per-category page budgets** after Emmanuel asked why we'd settle for "~40 calls in practice" when search quota is use-it-or-lose-it. The answer: that number was waste, not a choice — a uniform cap gives howto/tech_reviews pages they can't use (they run out of videos after 1–2) while vlogs/comedy truncate. Budgets now go where the videos are: vlogs 10 pages (500 results per arm/filter per daily window), comedy 8, product_reviews 5, howto/tech_reviews 3; worst-case search spend = Σ(queries × 2 filters × pages) = 84 calls/tick, under the 100-call budget with margin, ~50 in practice. `--discover-pages` becomes a uniform override for experiments only (default: per-category budgets). Re-tuning rule documented: an arm whose `search_rank` keeps hitting pages×50 in `cohort.csv` is still truncating and deserves more pages. New test pins every category's call count against its budget and the 84 total. Note for the live agent: it currently passes `--discover-pages 5` (a sensible override while `main` still defaults to 3); once this merges and is pulled, that argument must be removed so the per-category budgets apply. (Files: `02_Data/track_new_videos.py`, `02_Data/README.md`, `02_Data/FEATURES.md`)
