# Pinned to a recent build deliberately: yt-dlp<2026.08.19 fails all video
# downloads outright against YouTube's current anti-bot JS challenge
# ("unable to download video data: HTTP Error 403") -- confirmed by hitting
# it directly 2026-08-20. Also requires `deno` on PATH (brew install deno)
# for the JS-challenge solver collect_and_extract.py's YT_DLP_EXTRA_ARGS
# enables (--remote-components ejs:github).
yt-dlp==2026.8.19

# collect_and_extract.py -- visual + audio engineered features
# (additional_features.md). opencv pinned <5: opencv-python-headless 5.x
# dropped CascadeClassifier from the headless build (confirmed broken
# 2026-08-18); 4.x is the known-working line for the Haar-cascade face
# fallback used here.
numpy==2.2.6
opencv-python-headless>=4.8,<5
opensmile==2.6.0
webrtcvad==2.0.10

# Whisper fallback transcription (data_retrieval.md §4.2/4.3) for videos
# whose auto-captions are missing or fail the quality heuristic. Pulls in
# torch (CPU build) -- large download, but only install this happens once.
openai-whisper==20250625

# mediapipe intentionally NOT pinned here: mediapipe==1.0.1 (current PyPI
# release as of 2026-08-18) removed the `solutions.face_detection` API this
# script was written against (replaced by the Tasks API) and additionally
# pulls in a conflicting opencv-contrib-python build. The pipeline runs
# fine without it -- face detection falls back to the OpenCV Haar cascade
# above automatically. Revisit only if someone rewrites _face_detector()
# against mediapipe's Tasks API.
